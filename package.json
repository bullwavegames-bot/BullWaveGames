{
  "name": "bullwave-games",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend",
    "start": "npm run start --prefix frontend",
    "typecheck": "npm run typecheck --prefix frontend",
    "test:games": "npm run test:games --prefix frontend",
    "backend": "npm run dev --prefix backend",
    "backend:worker": "npm run dev:worker --prefix backend",
    "backend:build": "npm run build --prefix backend",
    "backend:typecheck": "npm run typecheck --prefix backend",
    "backend:migrate": "npm run migrate --prefix backend",
    "backend:seed": "npm run seed --prefix backend",
    "backend:test": "npm test --prefix backend"
  }
}

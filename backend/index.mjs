// Replaced by the TypeScript Fastify server in src/. Use `npm run backend` from the repo root.
console.error("This stub was replaced. Run `npm run backend` (Fastify + TypeScript in backend/src).");
process.exit(1);
